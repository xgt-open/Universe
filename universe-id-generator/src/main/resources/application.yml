
# universe-id-generator 配置
universe:
  id-generator:
    enabled: true  # 是否启用，默认true
    key-prefix: id_generator  # Redis key前缀，默认：id_generator
    default-key: id_generator_default  # 默认业务键
    expire-time: 0  # Redis key过期时间（秒），0表示永不过期

server:
  port: 8080
